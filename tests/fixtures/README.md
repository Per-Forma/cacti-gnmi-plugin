# Test fixtures

This directory contains synthetic, non-sensitive data used by the automated
test suite. Values use documentation-only addresses and placeholder
credentials where connection-shaped data is required.

Do not add production telemetry, device configurations, credentials,
certificates, private keys, or environment-specific filesystem paths.
